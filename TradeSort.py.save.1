import pprint
import str


class
data = open('doc', 'r')

result = open('result', 'w')

for info in data:
       stringSwap = vgdgh
       pprint.pprint(info)
       
       result.write(info)
       result.close()
